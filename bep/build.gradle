apply plugin: 'java-library'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':core')
    compile project(':relay')
    compile project(':http-relay')
    compile "net.jpountz.lz4:lz4:1.3.0"
}
